"""
Logistic regression model helpers.

This module will encapsulate:
- construction of scikit-learn LogisticRegression models with the
  hyperparameters used in the project
- convenience functions to fit and return trained models
- any task-specific wrappers that are genuinely unique to logistic
  regression (while leaving general training logic in `training.py`)
"""

